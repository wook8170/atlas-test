# atlas-test

ATLAS Agent Orchestration 라이브 검증용 샌드박스 저장소.

Initial commit — 에이전트 dispatch 시 base branch 로 사용.

## Current Seeded Contract

The repository now includes a local AGENTORC-5 follow-up scope contract:

- approved feature groups: guardian management mode, homework checklist, cross-device sync, server-backed notifications
- excluded categories: external calendar integrations, social/community features, billing, classroom operations, admin consoles

See `docs/agentorc-5-followup-scope.md` and `src/followupScope.ts`.
