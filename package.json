{
  "name": "atlas-test",
  "version": "0.0.1",
  "private": true,
  "description": "ATLAS Agent Orchestration sandbox",
  "type": "module",
  "scripts": {
    "build": "node --eval \"import('./src/index.ts')\"",
    "test": "node --test"
  }
}
