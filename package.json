{
  "name": "atlas-test",
  "version": "0.0.1",
  "private": true,
  "description": "ATLAS Agent Orchestration sandbox",
  "scripts": {
    "build": "tsc",
    "test": "node --test src/*.test.ts"
  }
}
